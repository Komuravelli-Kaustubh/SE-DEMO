def summation(n):
    if(n<10):
        return n
    else:
        digits=[int(digit) for digit in str(n)]
        temp=sum(digits)
        
        
        return summation(temp)

n=int(input())

result=summation(n)

print(result)
